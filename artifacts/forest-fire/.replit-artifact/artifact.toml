kind = "web"
previewPath = "/"
title = "Forest Fire Prediction Platform"
version = "1.0.0"
id = "artifacts/forest-fire"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19010

[services.development]
run = "pnpm --filter @workspace/forest-fire run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/forest-fire", "run", "build" ]
publicDir = "artifacts/forest-fire/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19010"
BASE_PATH = "/"
